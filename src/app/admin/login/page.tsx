"use client"
import { useRouter } from "next/navigation"; import { useState } from "react"; import { useAuthStore, DEMO_CREDENTIALS } from "@/store/auth-store"; import { Button } from "@/components/ui/button"
export default function Page(){const router=useRouter(),login=useAuthStore(s=>s.login),[error,setError]=useState('');return <div className="grid min-h-screen place-items-center bg-neutral-950 px-5 text-white"><form className="w-full max-w-md border border-white/15 p-8" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget),r=login(String(f.get('email')),String(f.get('password')));if(r.ok)router.push('/admin/dashboard');else setError(r.error??'Đăng nhập thất bại')}}><p className="text-xs uppercase tracking-[.25em] text-emerald-300">TOTO / Admin</p><h1 className="mt-3 font-display text-5xl font-bold uppercase">Đăng nhập</h1><p className="mt-3 text-sm text-white/50">Mock authentication — không dùng cho production.</p><div className="mt-7 space-y-4"><input name="email" type="email" required defaultValue={DEMO_CREDENTIALS.email} className="w-full border border-white/20 bg-transparent px-4 py-3"/><input name="password" type="password" required defaultValue={DEMO_CREDENTIALS.password} className="w-full border border-white/20 bg-transparent px-4 py-3"/></div>{error&&<p className="mt-3 text-sm text-red-400">{error}</p>}<Button className="mt-6 h-12 w-full bg-primary">Vào trang quản trị</Button><p className="mt-5 text-xs text-white/40">{DEMO_CREDENTIALS.email} / {DEMO_CREDENTIALS.password}</p></form></div>}
