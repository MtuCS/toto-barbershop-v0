"use client"
import { useRouter } from "next/navigation"
import { useState } from "react"
import { toast } from "sonner"
import { useAuthStore, DEMO_CREDENTIALS } from "@/store/auth-store"
import { Button } from "@/components/ui/button"
export default function Page(){const router=useRouter(),login=useAuthStore(s=>s.login),[error,setError]=useState(""),[busy,setBusy]=useState(false);return <div className="grid min-h-screen place-items-center bg-neutral-950 px-5 text-white"><form className="w-full max-w-md border border-white/15 p-8" onSubmit={async event=>{event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const result=await login(String(form.get("email")).trim(),String(form.get("password")).trim());if(result.ok){toast.success("Đăng nhập thành công");router.replace("/admin/dashboard");router.refresh()}else{setError(result.error??"Đăng nhập thất bại");setBusy(false)}}}><p className="text-xs uppercase tracking-[.25em] text-emerald-300">TOTO / Admin</p><h1 className="mt-3 font-display text-5xl font-bold uppercase">Đăng nhập</h1><p className="mt-3 text-sm text-white/50">Hệ thống quản trị ToTo Barbershop.</p><div className="mt-7 space-y-4"><label className="block text-xs uppercase tracking-wider text-white/50">Email<input name="email" type="email" required autoComplete="username" defaultValue={DEMO_CREDENTIALS.email} className="mt-2 w-full border border-white/20 bg-transparent px-4 py-3 text-white outline-none focus:border-[#287565]"/></label><label className="block text-xs uppercase tracking-wider text-white/50">Mật khẩu<input name="password" type="password" required autoComplete="current-password" defaultValue={DEMO_CREDENTIALS.password} className="mt-2 w-full border border-white/20 bg-transparent px-4 py-3 text-white outline-none focus:border-[#287565]"/></label></div>{error&&<p role="alert" className="mt-3 text-sm text-red-400">{error}</p>}<Button type="submit" disabled={busy} className="mt-6 h-12 w-full">{busy?"Đang xử lý...":"Vào trang quản trị"}</Button></form></div>}
